/**
 * Container types that items can be stored in
 */
export enum ContainerType {
    None = 0,
    Map = 1,
    Equipment = 2,
    Inventory = 3,
    Backpack = 4,
    Quickslot = 5,
    VaultPanel = 6,
    PotionPanel = 7
}

/**
 * The types of vault containers
 */
export enum VaultType {
    None = 0,
    Vault = 1,
    Gift = 2,
    Potions = 3
}
